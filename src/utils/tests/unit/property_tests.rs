#[cfg(test)]
mod tests {
    use objc2_foundation::NSObject;

    #[test]
    fn test_property_utils() {
        // The test is just a placeholder since we can't easily test PropertyUtils directly
        let _obj = NSObject::new();
    }
}
